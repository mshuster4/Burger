    
<div class="container">

    <div class="row">
        <div class="col-sm-12 text-center">
            <h1>Pizza Passport</h1>
        </div>
    </div>

    <div class="row">
        <div class="col-sm-6">

            <ul>
            {{#each pizza}}
                {{#unless this.devoured}}
                <li>
                    <p>
                        {{this.id}}: <span>Pizza Name: </span>{{this.pizza_name}} 
                        <button class="devour-button" data-id="{{id}}" data-devoured="{{devoured}}">Devour it!</button>
                    </p>
                </li>
                {{/unless}}
            {{/each}}
            </ul>

        </div>

        <div class="col-sm-6 devoured">
            {{#each pizza}}
                {{#if this.devoured}}
                <l1>
                    <p>{{this.id}}: <span class="label">Name of Pizza: </span>{{this.pizza_name}}</p>
                </l1>
                {{/if}}
            {{/each}}
        </div>
    </div>

    <form>
        <div class="form row">
            <div class="col-sm-12">
                <input type="text" id="pizza" class="form-control" placeholder="">
            </div>
        </div>
    </form>

    <div class="row">
        <div class="col-sm-12">
            <button id="submitButton" type="submit" class="btn btn-primary">Submit</button>
        </div>
    </div>

</div>

<script type="text/javascript">
    $(function(){

        $(".devour-button").on("click", function(event) {

            var id = $(this).data("id");
            var devoured = $(this).data("devoured");

            console.log(id);
            console.log(devoured);

            var wasDevoured = {
                devoured: 1
            };

            $.ajax("/api/pizzas/" + id, {
                type: "PUT",
                data: wasDevoured
            }).then(
                function() {
                    location.reload();
                }
            );
        });

        $("#submitButton").on("click", function(event) {
            event.preventDefault();

            var newPizza = {
                pizza_name: $("#pizza").val().trim(),
                devoured: 0
            };


            $.ajax("/api/pizzas/", {
                type: "POST",
                data: newPizza
            }).then(
                function() {
                    location.reload();
                }
            );
    
        });

    });

</script>


